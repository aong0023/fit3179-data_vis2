{
  "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
  "width": 650,
  "height": 500,
  "data": {
    "url": "https://raw.githubusercontent.com/aong0023/fit3179-data_vis2/refs/heads/main/Australia_slopes_by_beach.csv"
  },
  "params": [
    {
      "name": "state_param",
      "value": null,
      "bind": {
        "input": "select",
        "name": "State ",
        "options": [null, "WA", "NT", "QLD", "NSW", "VIC", "TAS", "SA"],
        "labels": ["All States", "WA", "NT", "QLD", "NSW", "VIC", "TAS", "SA"]
      }
    }
  ],
  "transform": [
    {
      "calculate": "datum.coastal_region === 'Central West WA' || datum.coastal_region === 'Kimberley' || datum.coastal_region === 'Southwest WA' || datum.coastal_region === 'Pilbara' || datum.coastal_region === 'Southern WA' ? 'WA' : datum.coastal_region === 'Eastern Cape York Peninsula' || datum.coastal_region === 'Southern Gulf of Carpentaria' || datum.coastal_region === 'Central Queensland' || datum.coastal_region === 'Western Cape York Peninsula' ? 'QLD' : datum.coastal_region === 'East Arnhem Land' || datum.coastal_region === 'Western NT' ? 'NT' : datum.coastal_region === 'South Australian Gulfs' || datum.coastal_region === 'Western Eyre Peninsula' || datum.coastal_region === 'Southern SA' || datum.coastal_region === 'Nullarbor' ? 'SA' : datum.coastal_region === 'East Tasmania' || datum.coastal_region === 'North Tasmania' || datum.coastal_region === 'West Tasmania' ? 'TAS' : datum.coastal_region === 'Southern NSW' || datum.coastal_region === 'Central East' ? 'NSW' : datum.coastal_region === 'Gippsland' || datum.coastal_region === 'Central and Western VIC' ? 'VIC' : 'Other'",
      "as": "state"
    },
    {
      "filter": "state_param === null || datum.state === state_param"
    }
  ],
  "mark": "bar",
  "encoding": {
    "x": {
      "field": "coastal_region",
      "type": "nominal",
      "title": "Coastal Region",
      "sort": "-y",
      "axis": {
        "labelAngle": 45,
        "labelFontSize": 16,
        "titleFontSize": 18,
        "labelColor": "#222",
        "titleColor": "#222",
        "labelLimit": 0, 
        "labelExpr": "replace(datum.label, ' ', '\\n')"
      }
    },
    "y": {
      "aggregate": "mean",
      "field": "beach_slope_average",
      "type": "quantitative",
      "title": "Average Beach Slope (°)",
      "axis": {
        "labelFontSize": 16,
        "titleFontSize": 18,
        "labelColor": "#222",
        "titleColor": "#222",
        "grid": true
      }
    },
    "color": {
      "field": "coastal_region",
      "type": "nominal",
      "legend": null
    },
    "tooltip": [
  {"field": "coastal_region", "type": "nominal", "title": "Region"},
  {
    "aggregate": "mean",
    "field": "beach_slope_average",
    "type": "quantitative",
    "title": "Avg. Slope (°)",
    "format": ".3f"
  },
  {"field": "state", "type": "nominal", "title": "State"}
    ]
  },
  "config": {
    "axis": {
      "labelFont": "Arial",
      "titleFont": "Arial",
      "labelFontSize": 16,
      "titleFontSize": 18,
      "labelColor": "#222",
      "titleColor": "#222",
      "tickSize": 6,
      "domainWidth": 2,
      "tickWidth": 2
    },
    "background": "#fff8dc"
  }
}
