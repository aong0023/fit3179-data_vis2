{
  "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
  "width": 700,
  "height": 500,
  "data": {
    "url": "https://raw.githubusercontent.com/aong0023/fit3179-data_vis2/refs/heads/main/Australia_slopes_by_beach.csv"
  },
  "params": [
    {
      "name": "state_param",
      "value": null,
      "bind": {
        "input": "select",
        "name": "State ",
        "options": [null, "WA", "NT", "QLD", "NSW", "VIC", "TAS", "SA"],
        "labels": ["All States", "WA", "NT", "QLD", "NSW", "VIC", "TAS", "SA"]
      }
    }
  ],
  "transform": [
    {
      "calculate": "datum.coastal_region === 'Central West WA' || datum.coastal_region === 'Kimberley' || datum.coastal_region === 'Southwest WA' || datum.coastal_region === 'Pilbara' || datum.coastal_region === 'Southern WA' ? 'WA' : datum.coastal_region === 'Eastern Cape York Peninsula' || datum.coastal_region === 'Southern Gulf of Carpentaria' || datum.coastal_region === 'Central Queensland' || datum.coastal_region === 'Western Cape York Peninsula' ? 'QLD' : datum.coastal_region === 'East Arnhem Land' || datum.coastal_region === 'North Arnhem Land' || datum.coastal_region === 'Western NT' ? 'NT' : datum.coastal_region === 'South Australian Gulfs' || datum.coastal_region === 'Western Eyre Peninsula' || datum.coastal_region === 'Southern SA' || datum.coastal_region === 'Nullarbor' ? 'SA' : datum.coastal_region === 'East Tasmania' || datum.coastal_region === 'North Tasmania' || datum.coastal_region === 'West Tasmania' ? 'TAS' : datum.coastal_region === 'Southern NSW' || datum.coastal_region === 'Central East' ? 'NSW' : datum.coastal_region === 'Gippsland' || datum.coastal_region === 'Central and Western VIC' ? 'VIC' : 'Other'",
      "as": "state"
    },
    {
      "filter": "state_param === null || datum.state === state_param"
    }
  ],
  "encoding": {
    "x": {
      "field": "coastal_region",
      "type": "nominal",
      "title": "Coastal Region",
      "axis": {
        "labelAngle": 45,
        "labelFontSize": 14,
        "titleFontSize": 16,
        "labelLimit": 0
      },
      "sort": "-y"
    },
    "y": {
      "aggregate": "mean",
      "field": "beach_slope_average",
      "type": "quantitative",
      "title": "Average Beach Slope (°)"
    },
    "color": {
      "field": "coastal_region",
      "type": "nominal",
      "legend": null
    },
    "tooltip": [
      {"field": "coastal_region", "type": "nominal", "title": "Region"},
      {
        "aggregate": "mean",
        "field": "beach_slope_average",
        "type": "quantitative",
        "title": "Avg. Slope (°)",
        "format": ".3f"
      },
      {"field": "state", "type": "nominal", "title": "State"}
    ]
  },
  "layer": [
    {
      "mark": {"type": "rule", "strokeWidth": 2},
      "encoding": {
        "y": {
          "aggregate": "mean",
          "field": "beach_slope_average",
          "type": "quantitative"
        }
      }
    },
    {
      "mark": {"type": "circle", "size": 120, "filled": true}
    }
  ],
  "config": {
    "axis": {
      "labelFont": "Arial",
      "titleFont": "Arial",
      "labelFontSize": 14,
      "titleFontSize": 16
    },
    "background": "#fff8dc"
  }
}
